export function transferTabs(section,count=0){return `<div class="section-tabs" aria-label="Transfermarkt">${[['search','Gericht zoeken'],['shortlist','Shortlist'],['clubs','Clubtransfers'],['incoming',`Ontvangen biedingen${count?` (${count})`:''}`],['scouting','Scoutingrapport']].map(([id,label])=>`<button class="formation ${section===id?'active':''}" data-transfer-section="${id}">${label}</button>`).join('')}</div>`;}
