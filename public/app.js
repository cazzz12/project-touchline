import { formations, lineUp } from './engine.js';
import { KEY, cost, newGame, newSeason, nextFixture, playRound, rating, schedule, scout, signPlayer, standings, train } from './game.js';
const $=s=>document.querySelector(s), money=n=>Number(n).toLocaleString('nl-NL');
const esc=x=>String(x).replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
let game;try{game=JSON.parse(localStorage.getItem(KEY));if(game?.version!==2||!Array.isArray(game.clubs))game=null;}catch{game=null;}
let tab='overview',last=null,message='';const save=()=>localStorage.setItem(KEY,JSON.stringify(game));
const tabs={overview:['◈','Overzicht'],squad:['♟','Selectie'],tactics:['◎','Tactiek'],training:['↗','Training'],transfers:['◇','Scouting & transfers'],league:['▤','Competitie']};
function newClub(){return `<div class="create"><div class="create-visual"><div class="giant-mark">◈</div><h2>EVERY LEGEND<br>STARTS SOMEWHERE.</h2></div><form id="create-form" class="create-form"><div class="brand">◈ TOUCHLINE <small>MANAGER</small></div><p class="kicker">JOUW VERHAAL BEGINT HIER</p><h1>Bouw een club.<br><em>Schrijf geschiedenis.</em></h1><p class="muted">Kies je identiteit, stel je elftal op en klim naar de top van de competitie.</p><label>CLUBNAAM<input name="name" maxlength="32" required value="Amsterdam Athletic"></label><label>CLUBKLEUR</label><div class="colors">${['#c5ff70','#79b8ff','#ff8672','#e2b8ff','#ffda72'].map((c,i)=>`<label><input type="radio" name="color" value="${c}" ${i?'':'checked'}><span style="--choice:${c}"></span></label>`).join('')}</div><button class="primary">CREËER MIJN CLUB ↗</button><small class="muted">Gratis offline prototype · Opgeslagen in deze browser</small></form></div>`;}
function shell(content,title,eyebrow){return `<div class="shell"><aside class="sidebar"><div class="brand">◈ TOUCHLINE <small>MANAGER</small></div><p class="season">● &nbsp; SEIZOEN 01 · BRONZE LEAGUE</p><nav>${Object.entries(tabs).map(([key,[icon,label]])=>`<button data-tab="${key}" class="nav ${tab===key?'selected':''}"><span>${icon}</span> ${label}</button>`).join('')}</nav><div class="manager"><span class="crest" style="--club:${game.color}">${esc(game.clubs[0].name.slice(0,2).toUpperCase())}</span><div><b>${esc(game.clubs[0].name)}</b><small>CLUBMANAGER</small></div></div></aside><main><header><div><p class="kicker">${eyebrow}</p><h1>${title}</h1></div><div class="wallet"><span>◈</span><div><small>CLUBKAS</small><b>${money(game.credits)}</b></div></div></header>${content}<footer>TOUCHLINE · OFFLINE PROTOTYPE · GEEN ECHT GELD OF WALLET</footer></main></div>${message?`<div class="toast">${esc(message)}</div>`:''}${last?resultModal():''}`;}
function overview(){const fixture=nextFixture(game),other=fixture?game.clubs[fixture[0]===0?fixture[1]:fixture[0]]:null,row=standings(game).find(r=>r.i===0),place=standings(game).findIndex(r=>r.i===0)+1;return shell(`<section class="hero"><div><p class="kicker">SPEELDAG ${Math.min(game.round+1,10)} / 10 · BRONZE LEAGUE</p><h2>${fixture?'De volgende wedstrijd wacht.':'Seizoen voltooid.'}<br><em>${fixture?'Laat je tactiek spreken.':'Op naar de volgende ronde.'}</em></h2><p>${fixture?`Je speelt ${fixture[0]===0?'thuis tegen':'uit bij'} ${esc(other.name)}. Bereid je selectie voor.`:'Bekijk de eindstand en begin een nieuw seizoen met dezelfde club.'}</p><button class="primary" data-action="${fixture?'play':'season'}">${fixture?'SPEEL WEDSTRIJD':'START NIEUW SEIZOEN'} &nbsp; ↗</button></div><div class="hero-crest" style="--club:${game.color}">${esc(game.clubs[0].name.slice(0,2).toUpperCase())}</div></section><div class="tiles"><div class="tile"><small>COMPETITIEPOSITIE</small><strong>#${place}<i> / 6</i></strong><span>${game.round} van 10 wedstrijden</span></div><div class="tile"><small>PUNTEN</small><strong>${row.pts}<i> PTS</i></strong><span>Elke wedstrijd telt</span></div><div class="tile"><small>TEGENSTANDER</small><strong class="short">${fixture?esc(other.name):'—'}</strong><span>Speeldag ${Math.min(game.round+1,10)}</span></div><div class="tile"><small>SELECTIE</small><strong>${game.clubs[0].players.length}<i> SPELERS</i></strong><span>11 spelers in de basis</span></div></div><div class="columns"><section class="card"><div class="card-head"><div><p class="kicker">ROAD TO GLORY</p><h3>Laatste uitslagen</h3></div><button class="link" data-tab="league">Ranglijst →</button></div>${game.results.filter(r=>r.home===0||r.away===0).slice(-4).reverse().map(r=>`<div class="row"><span>${esc(game.clubs[r.home===0?r.away:r.home].name)}</span><b>${r.goals[0]} – ${r.goals[1]}</b></div>`).join('')||'<p class="muted">Nog geen wedstrijden gespeeld. De aftrap is aan jou.</p>'}</section><section class="card"><p class="kicker">CLUBNIEUWS</p><h3>In de kleedkamer</h3>${game.news.slice(0,4).map(n=>`<div class="news">↗ &nbsp; ${esc(n)}</div>`).join('')}</section></div>`,'Jouw dug-out','HET SPEL BEGINT HIER');}
function squad(){const xi=lineUp(game.clubs[0],game.tactics.formation),ids=new Set(xi.map(p=>p.id)),players=[...game.clubs[0].players].sort((a,b)=>Number(ids.has(b.id))-Number(ids.has(a.id))||rating(b)-rating(a));return shell(`<div class="intro"><h2>Jouw elftal</h2><p>De beste spelers voor ${game.tactics.formation} worden automatisch opgesteld. Conditie en positie tellen mee.</p></div><div class="squad-grid"><div class="pitch"><div class="pitch-circle"></div><div class="pitch-box"></div><div class="pitch-players">${xi.slice().reverse().map(p=>`<div class="pitch-player"><span>${p.position}</span><b>${esc(p.name.split(' ').at(-1))}</b></div>`).join('')}</div></div><div class="card"><p class="kicker">STARTING XI</p><h3>Basisopstelling</h3>${xi.map(p=>`<div class="player-line"><span class="position">${p.position}</span><span>${esc(p.name)}</span><b>${rating(p)}</b></div>`).join('')}</div></div><section class="card"><p class="kicker">DE VOLLEDIGE SELECTIE</p><h3>Alle ${players.length} spelers</h3><div class="table-scroll"><table><thead><tr><th>SPELER</th><th>POS</th><th>LEEFTIJD</th><th>RATING</th><th>CONDITIE</th><th>STATUS</th></tr></thead><tbody>${players.map(p=>`<tr><td><b>${esc(p.name)}</b></td><td>${p.position}</td><td>${p.age}</td><td><span class="position">${rating(p)}</span></td><td><div class="bar"><span style="width:${p.fitness}%"></span></div>${p.fitness}%</td><td>${ids.has(p.id)?'<span class="badge">BASIS</span>':'RESERVE'}</td></tr>`).join('')}</tbody></table></div></section>`,'Mijn selectie','DE MENSEN OP HET VELD');}
function tactics(){return shell(`<div class="intro"><h2>Bepaal je speelstijl</h2><p>Meer aanval geeft gemiddeld meer kansen. Hoog tempo en druk zetten kosten balzekerheid.</p></div><div class="columns"><section class="card"><p class="kicker">BASISPLAN</p><h3>Formatie</h3><div class="formations">${Object.keys(formations).map(f=>`<button data-formation="${f}" class="formation ${game.tactics.formation===f?'active':''}">◈ &nbsp; ${f}</button>`).join('')}</div><div class="mini-pitch">${formations[game.tactics.formation].map((p,i)=>`<span style="left:${i===0?50:12+((i-1)%5)*19}%;top:${i===0?84:63-Math.floor((i-1)/5)*44}%">${p}</span>`).join('')}</div></section><section class="card"><p class="kicker">AANWIJZINGEN</p><h3>Spelinstellingen</h3>${[['mentality','Mentaliteit','Verdedigend','Aanvallend'],['pressing','Pressing','Afwachten','Druk zetten'],['tempo','Tempo','Geduldig','Direct']].map(([key,title,low,high])=>`<div class="slider"><div><b>${title}</b><strong>${game.tactics[key]}</strong></div><input type="range" min="0" max="100" value="${game.tactics[key]}" data-tactic="${key}"><small><span>${low}</span><span>${high}</span></small></div>`).join('')}<p class="muted">Wijzigingen worden direct opgeslagen voor de volgende wedstrijd.</p></section></div>`,'Tactisch bord','IEDERE KEUZE TELT');}
function training(){return shell(`<div class="intro"><h2>Plan een trainingssessie</h2><p>Eén sessie per speeldag. Ontwikkel vaardigheden of laat je spelers herstellen.</p></div><div class="training-grid">${[['Recovery','✦','Herstel','+9 conditie voor de selectie'],['Attacking','↗','Aanval','+1 aanval, −4 conditie'],['Defending','⬡','Verdediging','+1 verdediging, −4 conditie'],['Fitness','◉','Fysiek','+1 uithoudingsvermogen, +3 conditie']].map(([key,icon,title,desc])=>`<button class="training" data-train="${key}" ${game.trainingUsed?'disabled':''}><span class="training-icon">${icon}</span><strong>${title}</strong><small>${desc}</small><span>${game.trainingUsed&&game.training===key?'AFGEROND':'KIES SESSIE →'}</span></button>`).join('')}</div><div class="card"><h3>Coach tip</h3><p class="muted">Na een wedstrijd verliezen basisspelers conditie. Herstel je elftal of train vaardigheden en accepteer vermoeidheid.</p></div>`,'Trainingsveld','BOUW AAN MORGEN');}
function transfers(){return shell(`<section class="hero scout-hero"><div><p class="kicker">VERSTERK JE CLUB</p><h2>Talent wacht<br><em>op niemand.</em></h2><p>Ontdek vier jonge spelers. Een scoutingrapport kost 15.000 credits en is geldig tot je volgende wedstrijd.</p><button class="primary" data-action="scout" ${game.scout||game.credits<15000?'disabled':''}>${game.scout?'RAPPORT ONTVANGEN':'STUUR SCOUTS · 15.000 ◈'}</button></div><div class="hero-icon">◇</div></section><div class="intro"><h2>Scoutingrapport</h2><p>${game.market.length} beschikbare talenten</p></div><div class="market-grid">${game.market.length?game.market.map(p=>`<div class="market-card"><div class="market-meta"><span class="position">${p.position}</span><span>LEEFTIJD ${p.age}</span></div><div class="portrait">${esc(p.name[0])}</div><h3>${esc(p.name)}</h3><p>RATING <b>${rating(p)}</b> · POTENTIEEL ONBEKEND</p><div class="attrs"><span>AAN ${p.attack}</span><span>PAS ${p.passing}</span><span>VER ${p.defending}</span></div><button data-sign="${esc(p.id)}" ${game.credits<cost(p)?'disabled':''}>TEKEN · ${money(cost(p))} ◈</button></div>`).join(''):'<div class="empty"><span>◇</span><h3>Nog geen rapport</h3><p>Stuur scouts op pad om nieuwe spelers te ontdekken.</p></div>'}</div>`,'Scoutingsnetwerk','ONTDEK DE VOLGENDE STER');}
function league(){const rows=standings(game),day=schedule()[game.round];return shell(`<div class="intro"><h2>Het klassement</h2><p>Zes fictieve clubs spelen thuis en uit. Een hogere eindpositie levert een grotere bonus op.</p></div><div class="columns league-grid"><section class="card"><p class="kicker">LIVE STAND</p><div class="table-scroll"><table><thead><tr><th>#</th><th>CLUB</th><th>G</th><th>W</th><th>D</th><th>V</th><th>DS</th><th>PT</th></tr></thead><tbody>${rows.map((r,i)=>`<tr class="${r.i===0?'mine':''}"><td>${i+1}</td><td><b>${esc(r.name)}</b></td><td>${r.p}</td><td>${r.w}</td><td>${r.d}</td><td>${r.l}</td><td>${r.gf-r.ga}</td><td><b>${r.pts}</b></td></tr>`).join('')}</tbody></table></div></section><section class="card"><p class="kicker">${day?'VOLGENDE SPEELDAG':'EINDSTAND'}</p><h3>${day?`Ronde ${game.round+1}`:'Seizoen afgerond'}</h3>${day?day.map(([a,b])=>`<div class="fixture"><span>${esc(game.clubs[a].name)}</span><b>VS</b><span>${esc(game.clubs[b].name)}</span></div>`).join(''):'<p class="muted">Start een nieuw seizoen op het overzicht.</p>'}<h3>Recente uitslagen</h3>${game.results.slice(-8).reverse().map(r=>`<div class="fixture"><span>${esc(game.clubs[r.home].name)}</span><b>${r.goals[0]}–${r.goals[1]}</b><span>${esc(game.clubs[r.away].name)}</span></div>`).join('')||'<p class="muted">Nog geen wedstrijden gespeeld.</p>'}</section></div>`,'Bronze League','DE STRIJD OM DE TOP');}
function resultModal(){const r=last,stats=r.detail.stats,side=r.home===0?0:1;return `<div class="modal-backdrop"><div class="modal"><button class="close" data-action="close">✕</button><p class="kicker">FULL TIME · SPEELDAG ${r.round}</p><div class="score"><span>${esc(game.clubs[r.home].name)}</span><strong>${r.goals[0]} : ${r.goals[1]}</strong><span>${esc(game.clubs[r.away].name)}</span></div><h2>${r.goals[side]>r.goals[1-side]?'OVERWINNING!':r.goals[side]===r.goals[1-side]?'GELIJKSPEL':'VOLGENDE KEER BETER'}</h2>${[['Balbezit','possession','%'],['Schoten','shots',''],['xG','xg',''],['Passnauwkeurigheid','passAccuracy','%']].map(([label,key,suffix])=>`<div class="stat"><b>${stats[0][key]}${suffix}</b><span>${label}</span><b>${stats[1][key]}${suffix}</b></div>`).join('')}<h3>Wedstrijdmomenten</h3><div class="feed">${r.detail.events.filter(e=>e.type==='goal').map(e=>`<p><b>${e.minute}' ⚽</b> ${esc(e.player)} scoort voor ${esc(r.detail.clubs[e.side])}</p>`).join('')||'<p>Geen doelpunten deze wedstrijd.</p>'}</div><button class="primary" data-action="close">VERDER MET MIJN CLUB →</button></div></div>`;}
function render(){if(!game){$('#app').innerHTML=newClub();return;}$('#app').innerHTML=({overview,squad,tactics,training,transfers,league})[tab]();}
$('#app').addEventListener('submit',e=>{if(e.target.id!=='create-form')return;e.preventDefault();const data=new FormData(e.target);game=newGame(data.get('name'),data.get('color'));save();render();});
$('#app').addEventListener('click',e=>{const b=e.target.closest('button');if(!b||!game)return;if(b.dataset.tab){tab=b.dataset.tab;message='';render();return;}if(b.dataset.formation){game.tactics.formation=b.dataset.formation;save();render();return;}if(b.dataset.train){message=train(game,b.dataset.train)?'Training afgerond!':'Je hebt vandaag al getraind.';save();render();return;}if(b.dataset.sign){message=signPlayer(game,b.dataset.sign)?'Nieuwe speler aangetrokken!':'Niet genoeg credits of geen plek.';save();render();return;}if(b.dataset.action==='play'){last=playRound(game);message='';save();render();}if(b.dataset.action==='season'){if(newSeason(game)){last=null;message='Nieuw seizoen gestart!';save();render();}}if(b.dataset.action==='scout'){message=scout(game)?'Scoutingrapport ontvangen!':'Niet genoeg credits of al gescout.';save();render();}if(b.dataset.action==='close'){last=null;render();}});
$('#app').addEventListener('input',e=>{if(e.target.dataset.tactic&&game){game.tactics[e.target.dataset.tactic]=Number(e.target.value);e.target.closest('.slider').querySelector('strong').textContent=e.target.value;save();}});
render();
